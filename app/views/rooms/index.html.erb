<h1>Rooms#index</h1>

<% @rooms.each do |room| %>

  <%= image_tag "room.jpg", alt: "alttext", class: "card-image" %>
  <h5><%= room.name %></h5>
  <p><%= room.description.truncate(50) %></p>
  <p><%= link_to "See details", room_path(room) %></p>
  <p>£<%= room.cost %></p>

 <% end %>
